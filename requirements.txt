Flask==3.0.0
requests==2.31.0
beautifulsoup4==4.12.2
newspaper3k==0.2.8
feedparser==6.0.11  # For RSS fallback if scraping fails
transformers==4.35.0
torch==2.1.0  # CPU version; use torch==2.1.0+cu118 for GPU
nltk==3.8.1
openai==1.3.0  # Optional: For GPT-based SEO titles
