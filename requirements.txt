# Core Flask and web deps
Flask==3.1.2
requests==2.32.3
beautifulsoup4==4.12.3

# Scraping and RSS
newspaper3k==0.2.8
feedparser==6.0.11

# AI/NLP deps
transformers==4.44.0
nltk==3.8.2

# Optional SEO
openai==1.50.0

# PyTorch CPU-only (install separately via build command)
# torch==2.5.0+cpu handled in render.yml
