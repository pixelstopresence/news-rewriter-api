# PyTorch CPU-only installation (add this line first for wheel index)
--index-url https://download.pytorch.org/whl/cpu

# Core PyTorch deps (CPU versions)
torch==2.5.0+cpu
# torchvision==0.20.0+cpu  # Uncomment if using image processing

# Flask and web deps
Flask==3.0.0
requests==2.31.0
beautifulsoup4==4.12.2

# Scraping and RSS
newspaper3k==0.2.8
feedparser==6.0.11

# AI/NLP deps (compatible with torch 2.5.0)
transformers==4.35.0
nltk==3.8.1

# Optional advanced SEO
openai==1.3.0
